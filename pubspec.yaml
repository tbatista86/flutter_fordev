name: fordev
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  http: 
  provider:
  equatable:
  get:
  flutter_secure_storage:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons:
  test:
  mockito:
  faker:

flutter:
  uses-material-design: true
  assets:
    - lib/UI/assets/
  
flutter_icons:
  ios: true
  android: true
  image_path_android: lib/UI/assets/icon/android.png
  image_path_ios: lib/UI/assets/icon/ios.png
  adaptive_icon_backgroud: lib/UI/assets/icon/backgroud.png
  adaptive_icon_foreground: lib/UI/assets/icon/foregroung.png
